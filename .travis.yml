language:
  - c

os:
  - linux

sudo:
  - true

addons:
  apt:
    packages:
      - cmake
      - libav-tools
      - python3-pip

install:
  - sudo pip3 install pyacoustid pytaglib

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - ctest --output-on-failure
